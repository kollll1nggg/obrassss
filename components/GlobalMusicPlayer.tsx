import React from 'react';

// This component is no longer used as the music is now controlled
// directly from the MusicPage DJ interface.
const GlobalMusicPlayer: React.FC = () => {
  return null;
};

export default GlobalMusicPlayer;
